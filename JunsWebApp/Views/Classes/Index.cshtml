@model IEnumerable<JunsWebApp.Models.Class>

@{
    ViewBag.Title = "Classes Detail";
}

<h2>Classes below listed:</h2>

<p>
    @Html.ActionLink("Create New Class", "Create")
</p>

@*<form method="get">
    <input type ="search" name="searchTerm"/>
    <input type="submit" value="Search"/>
</form>*@

<form method="get" action="@Url.Action("Index")" data-jun-ajax="true" 
      data-jun-target="#classlistbody">
    <input type="search" name="searchTerm" data-jun-autocomplete="@Url.Action("AutoComplete")"/>
    <input type="submit" value="Search" />
</form>

    @Html.Partial("_Classes", Model)
